#!/bin/sh

MAUDE_LIB=$srcdir/../../src/Mixfix
export MAUDE_LIB

../../src/Mixfix/maude \
  < $srcdir/metaMatch.maude -no-banner \
  > metaMatch.out 2>&1

diff $srcdir/metaMatch.expected metaMatch.out > /dev/null 2>&1
